"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function NewCertificatePage() {
  const router = useRouter(); const [name, setName] = useState(""); const [orientation, setOrientation] = useState("landscape"); const [message, setMessage] = useState("");
  async function submit(event: React.FormEvent) { event.preventDefault(); const response = await fetch("/api/certificate-templates", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, orientation }) }); const data = await response.json(); if (!response.ok) return setMessage(data.message || "Erro ao criar."); router.push(`/admin/documentos/modelos/certificados/${data.template.id}/editar`); }
  return <div className="max-w-xl"><h1 className="text-2xl font-semibold">Novo certificado visual</h1><p className="mt-1 text-sm text-gray-600">Escolha o formato inicial. O layout poderá ser editado em seguida.</p><form onSubmit={submit} className="mt-6 space-y-4 rounded-lg border bg-white p-6"><label className="block text-sm">Nome<input required maxLength={200} className="mt-1 w-full rounded border px-3 py-2" value={name} onChange={(e) => setName(e.target.value)} /></label><label className="block text-sm">Formato<select className="mt-1 w-full rounded border px-3 py-2" value={orientation} onChange={(e) => setOrientation(e.target.value)}><option value="landscape">A4 paisagem</option><option value="portrait">A4 retrato</option></select></label>{message && <p className="text-sm text-red-600">{message}</p>}<button className="rounded bg-violet-700 px-4 py-2 text-sm font-medium text-white">Criar e editar</button></form></div>;
}
